const analytics = require('./analytics')
const concertHalls = require('./concert-halls')
const eventsExamples = require('./events-examples')
const events = require('./events')
const users = require('./users')

module.exports = {
  analytics,
  concertHalls,
  eventsExamples,
  events,
  users
}
